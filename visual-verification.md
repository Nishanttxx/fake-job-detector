# Visual verification notes

The desktop screening workspace renders with the intended navy, cream, and terracotta editorial palette. The left navigation, hero panel, four metric cards, input form, and result panel align cleanly at the 1280px viewport. Live overview metrics populate from the persisted artifact bundle after the server query resolves. The initial empty-state result panel is legible and provides a clear next action.

The dashboard intentionally uses a public, non-auth-gated shell because screening is the primary workflow. The model status card and disclaimer communicate that predictions are decision support rather than proof.
